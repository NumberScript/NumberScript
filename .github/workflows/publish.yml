name: Publish Extension

on:
  push:
    branches:
      - main

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'

      - name: Commit uncommitted changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff-index --quiet HEAD || git commit -m "chore: commit uncommitted changes [skip ci]"

      - name: Install dependencies
        run: npm install

      - name: Bump version
        run: npm version patch -m "CI: Bump version to %s [skip ci]"

      - name: Compile/Build Extension
        run: npm run build

      - name: Package VS Code Extension
        run: npx @vscode/vsce package

      # Optional: Publish to VS Code Marketplace
      # - name: Publish Extension
      #   run: npx @vscode/vsce publish
      #   env:
      #     VSCE_PAT: ${{ secrets.VSCE_PAT }}
